<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Redirecting...</title>
    <script type="text/javascript">
        // Only redirect to your own domain
        const basePath = window.location.origin + 
                        (window.location.pathname.includes('/turbo-chainsaw') ? '/turbo-chainsaw' : '');
        
        // Don't pass the original path as a parameter (security risk)
        // Let your SPA router handle it from window.location
        window.location.replace(basePath + '/index.html');
    </script>
</head>
<body>
    Redirecting...
</body>
</html>
